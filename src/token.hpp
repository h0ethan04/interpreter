#ifndef TOKEN_HPP
#define TOKEN_HPP

#include <string>

#include "location.hpp"

template <typename Type>
class GrinToken {
    public:

    GrinToken(GrinTokenKind kind, std::string text, GrinLocation location, <Type> data)

    private:

};

#endif